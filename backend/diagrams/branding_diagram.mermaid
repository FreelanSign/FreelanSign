classDiagram
    class BrandTheme {
        +UUID id
        +String name
        +Boolean is_active
        +JSON colors
        +JSON typography
        +JSON spacing
        +String logo
        +DateTime created_at
        +DateTime updated_at
    }
    BrandTheme "*" --> "1" User : professional