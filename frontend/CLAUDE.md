# Claude.md

## What AI MUST DO

- Always use Shadcn UI components when possible
- Always use Tailwind CSS classes when possible
- Make style global in `src/styles/globals.css` for global styles
- Use TypeScript for type safety
